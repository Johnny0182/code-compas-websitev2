# Server-side only. Never prefix these with NEXT_PUBLIC_.
# Create this in Resend → API Keys with Sending access.
RESEND_API_KEY=
# Must use a domain or subdomain verified in Resend.
# Example: Code Compas Website <inquiries@mail.yourdomain.com>
CONTACT_FROM_EMAIL=
# The inbox where new contact-form inquiries should arrive.
CONTACT_TO_EMAIL=
